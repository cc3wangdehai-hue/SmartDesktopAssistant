<Window x:Class="WidgetsStandalone.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:weather="clr-namespace:WeatherWidget;assembly=WeatherWidget"
        xmlns:todo="clr-namespace:TodoWidget;assembly=TodoWidget"
        Title="桌面小组件测试版"
        Height="400" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Weather Widget -->
        <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            <weather:WeatherControl x:Name="WeatherCtrl"/>
        </Border>

        <!-- TODO Widget -->
        <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            <todo:TodoControl x:Name="TodoCtrl"/>
        </Border>
    </Grid>
</Window>
