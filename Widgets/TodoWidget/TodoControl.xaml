<UserControl x:Class="TodoWidget.TodoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="280"
             Background="Transparent">
    
    <UserControl.Resources>
        <Style x:Key="TodoCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="checkBorder" Width="16" Height="16" 
                                    CornerRadius="3" BorderThickness="1.5"
                                    BorderBrush="#E53935" Background="Transparent"
                                    Margin="0,0,8,0">
                                <Path x:Name="checkMark" Visibility="Collapsed"
                                      Data="M2,5 L5,8 L12,2" 
                                      Stroke="#E53935" StrokeThickness="2"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkBorder" Property="Background" Value="#FFEBEE"/>
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="11">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="CaretBrush" Value="#E53935"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input Area -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtInput" Style="{StaticResource InputBox}"
                     Grid.Column="0" KeyDown="TxtInput_KeyDown"
                     ToolTip="输入待办，按回车添加"/>
            <Button x:Name="BtnSubmit" Grid.Column="1" Style="{StaticResource IconButton}"
                    Margin="6,0,0,0" Click="Add_Click" ToolTip="添加"
                    Background="#E53935" Foreground="White">
                <TextBlock Text="+" FontSize="14"/>
            </Button>
        </Grid>

        <!-- TODO List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="TodoList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,4" Padding="8,6"
                                Background="#FAFAFA" CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox Grid.Column="0" 
                                          IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                          Style="{StaticResource TodoCheckBox}"
                                          Click="Todo_CheckChanged"
                                          Tag="{Binding Id}"
                                          VerticalAlignment="Center"/>
                                
                                <StackPanel Grid.Column="1" Margin="4,0,8,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" Foreground="#333"
                                               FontFamily="Microsoft YaHei UI" FontSize="12"
                                               TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                        <Setter Property="Opacity" Value="0.5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:HH:mm}}"
                                               Foreground="#999" FontSize="9" Margin="0,1,0,0"/>
                                </StackPanel>
                                
                                <Button Grid.Column="2" Style="{StaticResource IconButton}"
                                        Click="Delete_Click" Tag="{Binding Id}"
                                        ToolTip="删除" Width="20" Height="20">
                                    <TextBlock Text="×" FontSize="14"/>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel x:Name="EmptyState" Grid.Row="1" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="Visible">
            <TextBlock Text="暂无待办事项" Foreground="#999" FontSize="12"
                       FontFamily="Microsoft YaHei UI" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Sync Button -->
        <Button x:Name="BtnSync" Grid.Row="2" Style="{StaticResource IconButton}"
                Click="Sync_Click" ToolTip="同步到飞书" Margin="0,8,0,0"
                HorizontalAlignment="Right" Background="#FFF3E0" Foreground="#E65100">
            <TextBlock Text="⇄" FontSize="12"/>
        </Button>
    </Grid>
</UserControl>
